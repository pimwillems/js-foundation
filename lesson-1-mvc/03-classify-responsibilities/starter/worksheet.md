# Werkblad

| Gedrag of fragment | M / V / C / gemengd | Waarom? |
|---|---|---|
| Controleren of `book.available` waar is voordat lenen mag | | |
| De CSS-class `available` kiezen om status groen te tonen | | |
| Een foutmelding in `#message` plaatsen | | |
| `Number(button.dataset.bookId)` uitvoeren | | |
| `books.find((book) => book.id === id)` | | |
| Een klik ontvangen en `model.borrowBook(id)` starten | | |
| `books.push(newBook); list.innerHTML = ...` | | |
| Bepalen dat een onbekend boek niet geleend kan worden | | |
